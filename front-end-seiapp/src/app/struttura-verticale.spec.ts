import { StrutturaVerticale } from './struttura-verticale';

describe('StrutturaVerticale', () => {
  it('should create an instance', () => {
    expect(new StrutturaVerticale()).toBeTruthy();
  });
});
