import { EdificioInAggregato } from './edificio-in-aggregato';

describe('EdificioInAggregato', () => {
  it('should create an instance', () => {
    expect(new EdificioInAggregato()).toBeTruthy();
  });
});
