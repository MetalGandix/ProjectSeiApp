import { CaratteristicheAssociazioneIntervento } from './caratteristiche-associazione-intervento';

describe('CaratteristicheAssociazioneIntervento', () => {
  it('should create an instance', () => {
    expect(new CaratteristicheAssociazioneIntervento()).toBeTruthy();
  });
});
