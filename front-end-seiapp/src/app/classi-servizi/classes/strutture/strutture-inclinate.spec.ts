import { StruttureInclinate } from './strutture-inclinate';

describe('StruttureInclinate', () => {
  it('should create an instance', () => {
    expect(new StruttureInclinate()).toBeTruthy();
  });
});
