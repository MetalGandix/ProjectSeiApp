import { CaratteristicheStruttura } from './caratteristiche-struttura';

describe('CaratteristicheStruttura', () => {
  it('should create an instance', () => {
    expect(new CaratteristicheStruttura()).toBeTruthy();
  });
});
